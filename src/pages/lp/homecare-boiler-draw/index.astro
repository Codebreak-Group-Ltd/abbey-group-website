---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Section from '../../../components/Section.astro';
import Eyebrow from '../../../components/Eyebrow.astro';
import Button from '../../../components/Button.astro';
import Icon from '../../../components/Icon.astro';
import PhotoSlot from '../../../components/PhotoSlot.astro';
import FAQ from '../../../components/FAQ.astro';
import EnquiryForm from '../../../components/EnquiryForm.astro';
import { nap, site, ratings, homecareReviews } from '../../../data/site.ts';

/* =====================================================================
   /lp/homecare-boiler-draw/ — Homecare BOILER-DRAW campaign landing page.
   Rebuilt 12 August 2026 as a lean, form-first giveaway lead-gen page
   (Josh): the first build read as a brochure. This one has ONE message and
   ONE action — the prize leads, the four-field form is in the hero above the
   fold, the deadline is prominent, and roughly half the old sections are gone.

   WHAT WAS CUT, and why it is fine: the plan-picker quiz, the three-plan
   pricing grid with buy buttons, the six-point service grid and the
   without/with comparison table all came out. A visitor here has clicked an
   advert and is weighing the prize and how easy it is to enter, not comparing
   subscription tiers. Everything cut still lives on `/homecare-plans/` and the
   evergreen `/lp/homecare/`, which are untouched.

   RELATIONSHIP TO `/lp/homecare/`. That is the EVERGREEN homecare LP, run all
   year and built to educate a problem-aware visitor. THIS is its time-boxed,
   conversion-first sibling for the boiler draw. They share the design system
   and the plan facts, not their structure.

   COMPLIANCE, LOAD-BEARING. The form is "get started, we will call you",
   never "you are now entered". Entry into the draw happens only on an actual
   paid sign-up (or a free postal entry), so the giveaway styling must not imply
   the form itself enters anyone — the hero form's own microcopy says so. The
   significant-conditions line, the free-postal-entry block with the office
   address, and the terms link are all present and prominent, not buried.
   No insurance/policy/claim/premium wording anywhere. British English, no
   em dashes, no exclamation marks.

   FUNNEL. A form submission is interest, not entry, so it posts to
   PUBLIC_GHL_WEBHOOK then to `/thank-you/` ("thanks, we have got your
   details") and joins the lead/nurture path. UTMs travel with it via
   EnquiryForm's read-at-submit attribution (no hidden fields, no device
   storage — the deliberate PECR-safe pattern, see EnquiryForm.astro).

   THE FORM SCRIPT IS SHARED. The hero form is hand-written for layout control,
   but the submit/webhook/redirect handler is EnquiryForm's global script,
   which wires every `form[data-source]` on the page. The final-CTA EnquiryForm
   brings that script; the hero form rides on it. Keep both `data-source` /
   `data-redirect` in sync if either changes.

   HELD, do not build yet: the self-serve sign-up/payment path and its "you are
   in" thank-you (pending Amy on site payment); the landlord variant (pending
   James). Tracking/consent is a separate task (Josh, 12 August 2026); nothing
   tracking-related fires here.

   PLACEHOLDERS pending the solicitor: the significant-conditions wording and
   the full-terms link (see the constants below). Pending the prize being
   confirmed: the hero prize image is a PhotoSlot, to be swapped for a clean
   product shot of the actual boiler on a white background. Launch also needs
   the live GHL webhook URL.
   ===================================================================== */

const title = 'Win a New Boiler | Homecare from £9.99 a Month | Abbey Group';
const description =
  'Join an Abbey Group homecare plan before 19 September to enter our free draw to win a new boiler. Gas Safe cover in Whitby from £9.99 a month.';

/* ---- Competition constants ----
   ⚠️ `conditionsLine` is PLACEHOLDER wording. Replace with the solicitor's
   exact short-form conditions before launch. The mechanic, the date and the
   free route are correct; only the precise legal phrasing is pending. */
const DRAW_CLOSE = '19 September 2026';
const DRAW_CLOSE_SHORT = '19 September';
const DEADLINE_ISO = '2026-09-19T23:59:59';
const conditionsLine =
  'No purchase necessary. Free postal entry available. One entry per person. Draw closes 19 September 2026. Terms apply.';
const TERMS_HREF = '#draw-terms';

/* Shared form attributes, so the hero form and the final-CTA form stay in step. */
const FORM_SOURCE = 'lp-homecare-boiler-draw';
const FORM_TAGS = 'homecare,boiler-cover,competition,comp-enquired';

/* Two verified reviews for the short trust block, verbatim from the master
   (convention 8). Angela evidences priority response, Dan the whole job. */
const trustReviews = homecareReviews.filter((r) => r.name === 'Angela' || r.name === 'Dan McDermott');

/* Short, competition-specific FAQ. Four questions, no more. Kept local rather
   than reusing the evergreen set because these answer the draw, not the plans.
   No page-level FAQPage schema is emitted (this is an LP). */
const drawFaqs = [
  {
    q: 'What does £9.99 a month cover?',
    a: 'Your annual boiler service, a gas safety certificate, a heating health check, priority response and one free call-out, every year you are covered. It is a service plan looked after by the same local team.',
  },
  {
    q: 'How do I enter the draw?',
    a: `Join any Abbey Group homecare plan before ${DRAW_CLOSE} and you are entered automatically once the plan is active. You do not have to join to enter: you can also enter free by post, with no plan needed.`,
  },
  {
    q: 'When is the draw?',
    a: `We draw one winner after entries close on ${DRAW_CLOSE} and fit their new boiler. It is a chance to win, not a guarantee.`,
  },
  {
    q: 'Can I cancel?',
    a: 'Yes, any time. There is no minimum term and no cancellation fee, and if you change your mind within 14 days you get a full refund.',
  },
];
---
<BaseLayout
  title={title}
  description={description}
  noindex={true}
  chromeless={true}
  division="homecare"
>
  <!-- 1. HERO — prize leads, form in the hero, deadline prominent.
       Grid areas rather than source order so the MOBILE stack (lead, panel,
       foot) puts the headline, deadline and form up top and above the fold,
       while DESKTOP keeps the copy left and the prize-and-form panel right. -->
  <section class="hero" id="entry">
    <div class="container hero__grid">
      <div class="hero__lead">
        <p class="hero__eyebrow">Abbey Group homecare · Whitby</p>
        <h1 class="hero__h1">Win a New Boiler for Your Home</h1>
        <!-- ⚠️ Add the real, verifiable prize value when confirmed
             (for example "a boiler supplied and fitted, worth £X"). Do NOT ship
             a placeholder figure — an unverifiable prize value is a compliance
             risk on a prize draw. -->
        <p class="hero__sub">
          Join an Abbey Group homecare plan before 19 September and you are entered into the draw
          for a brand new boiler, supplied and fitted. Cover starts at £9.99 a month.
        </p>

        <!-- Deadline, prominent. The label is the no-JS fallback; the countdown
             is filled in and revealed by the script at the foot of the page. -->
        <div class="cd" data-deadline={DEADLINE_ISO}>
          <span class="cd__label">Entries close {DRAW_CLOSE_SHORT}</span>
          <span class="cd__timer" hidden>
            <span class="cd__box"><b class="cd__d">0</b><i>days</i></span>
            <span class="cd__box"><b class="cd__h">0</b><i>hrs</i></span>
            <span class="cd__box"><b class="cd__m">0</b><i>mins</i></span>
          </span>
        </div>
      </div>

      <div class="hero__panel">
        <!-- ⚠️ PLACEHOLDER: swap for a clean product shot of the actual prize
             boiler on a white background once the prize is confirmed. Not a
             lifestyle kitchen photo. -->
        <PhotoSlot
          class="hero__prize"
          ratio="4 / 3"
          radius="lg"
          caption="Prize boiler image to follow: a clean product shot of the boiler on a white background"
        />

        <form
          class="hero__form"
          id="entry-form"
          data-source={FORM_SOURCE}
          data-tags={FORM_TAGS}
          data-redirect="/thank-you/"
          novalidate
        >
          <p class="hero__formhead">Enter in under a minute</p>
          <div class="hero__fields">
            <p class="hf">
              <label for="e-name">Full name <span class="hf__req" aria-hidden="true">*</span></label>
              <input id="e-name" name="name" type="text" autocomplete="name" required />
            </p>
            <p class="hf">
              <label for="e-phone">Phone <span class="hf__req" aria-hidden="true">*</span></label>
              <input id="e-phone" name="phone" type="tel" autocomplete="tel" required />
            </p>
            <p class="hf">
              <label for="e-email">Email <span class="hf__req" aria-hidden="true">*</span></label>
              <input id="e-email" name="email" type="email" autocomplete="email" required />
            </p>
            <p class="hf">
              <label for="e-postcode">Postcode</label>
              <input id="e-postcode" name="postcode" autocomplete="postal-code" />
            </p>
          </div>
          <Button type="submit" size="lg" block>Get Me Covered</Button>
          <!-- Compliance stays load-bearing: the form is a callback request, and
               entry happens on sign-up, not on submit. Friction/free-entry cue
               added per the change ticket (hero microcopy = risk removal). -->
          <p class="hero__formmicro">
            Takes under a minute, and it is free to enter by post too. We call you back to set it
            up: you enter the draw by joining a plan, not by sending this. Prefer to talk?
            <a href={`tel:${nap.phone.tel}`}>Call the office</a>.
          </p>
        </form>
      </div>

      <div class="hero__foot">
        <p class="hero__trust">
          <Icon name="star" size={16} />
          <span>{ratings.google.value} from {ratings.google.count} Google reviews · Whitby's own since 2008</span>
        </p>
        <p class="hero__cond">{conditionsLine} <a href={TERMS_HREF}>Full terms</a>.</p>
      </div>
    </div>
  </section>

  <!-- 2. HOW IT WORKS — three short steps. -->
  <Section bg="white">
    <div class="how__head reveal">
      <Eyebrow>How it works</Eyebrow>
      <h2 class="h2">Three steps to your chance at a new boiler</h2>
    </div>
    <ol class="how__steps reveal">
      <li class="how__step">
        <span class="how__num tnum" aria-hidden="true">1</span>
        <h3 class="how__t">Join a homecare plan</h3>
        <p class="how__d">Start any plan before {DRAW_CLOSE_SHORT}, from £9.99 a month. We set it up with you on a quick call.</p>
      </li>
      <li class="how__step">
        <span class="how__num tnum" aria-hidden="true">2</span>
        <h3 class="how__t">You are entered</h3>
        <p class="how__d">Once your plan is active we add you to the draw. One entry per person, and there is nothing else to fill in.</p>
      </li>
      <li class="how__step">
        <span class="how__num tnum" aria-hidden="true">3</span>
        <h3 class="how__t">Winner drawn {DRAW_CLOSE_SHORT}</h3>
        <p class="how__d">We draw one winner when entries close and fit their new boiler. A chance to win, not a guarantee.</p>
      </li>
    </ol>

    <!-- Free postal route, genuinely on the page (no purchase necessary). -->
    <div class="postal reveal">
      <div>
        <h3 class="postal__t">Prefer not to join? Enter free by post</h3>
        <p class="postal__d">
          Send a postcard with your name, address, phone number and email, marked
          <strong>Boiler Draw entry</strong>. One entry per person, and it must reach us before
          entries close on {DRAW_CLOSE}.
        </p>
      </div>
      <address class="postal__addr">{site.legalName}<br />{nap.addressFull}</address>
    </div>

    <div class="ctarow reveal">
      <Button href="#enquiry" size="lg">Get Me Covered</Button>
      <span class="ctarow__note">Entries close {DRAW_CLOSE_SHORT}</span>
    </div>
  </Section>

  <!-- 3. WHY ABBEY — short: the "not a call centre" line plus two real reviews. -->
  <Section bg="sunken">
    <div class="why">
      <div class="why__copy reveal">
        <Eyebrow>Who you are dealing with</Eyebrow>
        <h2 class="h2">A Whitby firm, not a call centre</h2>
        <p class="why__lead">
          When you ring, someone in the Skinner Street office picks up. When the engineer turns up,
          it is one of the same handful of people every year, looking after boilers here since 2008.
        </p>
        <ul class="why__facts">
          <li><Icon name="badge-check" size={16} /><span>Gas Safe registered, number {site.gasSafeNo}</span></li>
          <li><Icon name="badge-check" size={16} /><span>Working in Whitby since 2008</span></li>
          <li><Icon name="badge-check" size={16} /><span>{ratings.google.value} from {ratings.google.count} Google reviews</span></li>
        </ul>
      </div>
      <ul class="why__reviews reveal">
        {trustReviews.map((r) => (
          <li class="why__review">
            <p class="why__quote">{r.quote}</p>
            <p class="why__by"><span>{r.name}</span>, {r.source}</p>
          </li>
        ))}
      </ul>
    </div>

    <div class="ctarow reveal">
      <Button href="#enquiry" size="lg">Get Me Covered</Button>
      <span class="ctarow__note">Entries close {DRAW_CLOSE_SHORT}</span>
    </div>
  </Section>

  <!-- 4. THE OFFER — the £48 hook, with total clarity that it is TWO things. -->
  <Section bg="dark" bleed>
    <div class="offer">
      <img class="offer__mark" src="/brand/crescent/mark-homecare.svg" alt="" aria-hidden="true" width="360" height="378" />
      <div class="container offer__inner reveal">
        <div class="offer__copy">
          <Eyebrow tone="inverse">The joining offer</Eyebrow>
          <h2 class="offer__h2">Two things, and worth being clear about</h2>
          <p class="offer__lead">
            A one-off boiler service normally costs £96. Start a plan on the same visit and that
            service is £48. Separately, the plan itself is £9.99 a month, which covers your service
            every year after. The £48 is a one-off on the first visit. The £9.99 is the monthly
            cover. Two separate things, not one.
          </p>
          <p class="offer__guar">
            Try it for two weeks. If it is not for you, a full refund, with no minimum term and no
            fee to leave.
          </p>
          <p class="offer__pricing">Plans from £9.99 a month, and we will talk you through them on the call.</p>
        </div>

        <div class="offer__card">
          <div class="offer__row offer__row--was">
            <span class="offer__label">Boiler service on its own</span>
            <span class="offer__num tnum">£96</span>
          </div>
          <div class="offer__row offer__row--now">
            <span class="offer__label">Service on your joining visit</span>
            <span class="offer__num tnum">£48</span>
            <span class="offer__then">a one-off, on the first visit</span>
          </div>
          <div class="offer__row offer__row--plan">
            <span class="offer__label">Then the plan</span>
            <span class="offer__num tnum">£9.99<span class="offer__mo">/mo</span></span>
            <span class="offer__then">covers your service every year after</span>
          </div>
        </div>
      </div>

      <div class="container ctarow ctarow--dark reveal">
        <Button href="#enquiry" variant="primary" size="lg">Get Me Covered</Button>
        <span class="ctarow__note">Entries close {DRAW_CLOSE_SHORT}</span>
      </div>
    </div>
  </Section>

  <!-- 5. SHORT FAQ — four questions. FAQ renders its own section (white). -->
  <FAQ
    eyebrow="Good to know"
    heading="Four quick questions"
    items={drawFaqs}
    bg="white"
  />

  <!-- 6. FINAL CTA — the repeat form. This EnquiryForm also loads the global
       submit/webhook/redirect script the hero form rides on. Same source, tags
       and redirect as the hero form. -->
  <EnquiryForm
    eyebrow="Last step"
    heading="Get started, and get into the draw"
    lead={`Leave your details and we will ring you back to set your plan up or talk it through. Your entry counts from when your plan is active. Entries close ${DRAW_CLOSE_SHORT}.`}
    source={FORM_SOURCE}
    tags={FORM_TAGS}
    submitLabel="Get Me Covered"
    bg="sunken"
    redirect="/thank-you/"
    fields={[
      { name: 'name', label: 'Full name', required: true, autocomplete: 'name' },
      { name: 'phone', label: 'Phone', type: 'tel', required: true, autocomplete: 'tel' },
      { name: 'email', label: 'Email', type: 'email', required: true, autocomplete: 'email' },
      { name: 'postcode', label: 'Postcode', autocomplete: 'postal-code' },
    ]}
  >
    <ul class="ef__risk">
      <li><Icon name="check" size={15} /><span>No purchase necessary, enter free by post</span></li>
      <li><Icon name="check" size={15} /><span>Cancel any time, no minimum term</span></li>
      <li><Icon name="check" size={15} /><span>Full refund within 14 days</span></li>
    </ul>
    <p class="ef__talk">
      Prefer to talk? Call the Whitby office on
      <a class="tnum" href={`tel:${nap.phone.tel}`}>{nap.phone.label}</a>
      and a real person will answer.
    </p>
  </EnquiryForm>

  <!-- DRAW TERMS — the "Full terms" anchor target and the foot-of-page
       restatement of the significant conditions. Placeholder wording pending
       the solicitor; the significant conditions are carried in full so the page
       stands on its own, and `TERMS_HREF` is never a dead link. -->
  <Section bg="white" id="draw-terms">
    <div class="drawterms">
      <h2 class="drawterms__h2">Prize draw terms</h2>
      <p class="drawterms__line">{conditionsLine}</p>
      <ul class="drawterms__list">
        <li>Promoter: {site.legalName}, company no. {site.companyNo}, {nap.addressFull}.</li>
        <li>Enter by joining any Abbey Group homecare plan that is active before entries close on {DRAW_CLOSE}, or free by post to the address above.</li>
        <li>One entry per person. The prize is one new boiler, supplied and fitted by Abbey Group. No cash alternative.</li>
        <li>The winner is drawn after entries close and will be contacted using the details provided.</li>
        <li>Full terms and conditions are available from the office on request, and the complete written terms will be published here before the campaign goes live.</li>
      </ul>
    </div>
  </Section>
</BaseLayout>

<style>
  .h2 { font-size: clamp(30px, 3vw, 44px); color: var(--text-heading); margin-top: 10px; }

  /* ---- 1. HERO ---------------------------------------------------------- */
  .hero {
    position: relative;
    overflow: hidden;
    color: var(--white);
    padding: 120px var(--container-padding) 64px;
    background:
      radial-gradient(1000px 640px at 82% 12%, color-mix(in oklch, var(--page-accent-glow, var(--orange-900)) 60%, transparent) 0%, transparent 62%),
      radial-gradient(760px 520px at 4% 96%, color-mix(in oklch, var(--page-accent, var(--orange-500)) 14%, transparent) 0%, transparent 60%),
      var(--surface-page-dark, var(--ink-500));
  }
  .hero::after {
    content: '';
    position: absolute; inset: auto 0 0 0; height: 3px;
    background: var(--page-accent, var(--orange-500)); opacity: 0.9;
  }

  /* Desktop: copy left (lead over foot), prize+form panel right. Mobile: the
     source order (lead, panel, foot) stacks, so headline, deadline and form
     land above the fold. */
  .hero__grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.02fr 0.98fr;
    grid-template-areas: "lead panel" "foot panel";
    column-gap: 56px;
    row-gap: 28px;
    align-items: start;
  }
  .hero__lead { grid-area: lead; }
  /* Flex column so the prize image and form can be re-ordered when the hero
     stacks (mobile): form first, image below, so the form is reachable without
     scrolling (change ticket 1.4). Desktop keeps source order: image over form. */
  .hero__panel { grid-area: panel; display: flex; flex-direction: column; }
  .hero__foot { grid-area: foot; align-self: end; }

  .hero__eyebrow {
    font-size: 12.5px; font-weight: var(--weight-bold);
    letter-spacing: var(--tracking-wider); text-transform: uppercase;
    color: var(--text-muted-inverse);
  }
  .hero__h1 {
    margin-top: 14px;
    font-family: var(--font-display); font-weight: 400;
    font-size: clamp(38px, 4.6vw, 60px); line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight); text-wrap: balance; color: var(--white);
  }
  .hero__sub {
    margin-top: 18px; max-width: 46ch;
    font-size: var(--text-md); line-height: var(--leading-relaxed);
    color: var(--text-body-inverse);
  }

  /* Deadline / countdown */
  .cd {
    display: flex; flex-wrap: wrap; align-items: center; gap: 14px;
    margin-top: 24px; padding: 14px 18px;
    background: color-mix(in oklch, var(--white) 8%, transparent);
    border: 1px solid color-mix(in oklch, var(--page-accent, var(--orange-500)) 45%, transparent);
    border-radius: var(--radius-lg);
  }
  .cd__label {
    font-size: 14px; font-weight: var(--weight-bold);
    letter-spacing: var(--tracking-wide); text-transform: uppercase;
    color: var(--page-accent-on-dark, var(--orange-400));
  }
  .cd__timer { display: inline-flex; gap: 10px; }
  .cd__box {
    display: inline-flex; flex-direction: column; align-items: center;
    min-width: 46px; padding: 6px 8px;
    background: color-mix(in oklch, var(--white) 10%, transparent);
    border-radius: var(--radius-md);
  }
  .cd__box b { font-size: 20px; font-weight: var(--weight-bold); line-height: 1; color: var(--white); font-family: var(--font-body); }
  .cd__box i { margin-top: 3px; font-style: normal; font-size: 10px; letter-spacing: var(--tracking-wide); text-transform: uppercase; color: var(--text-muted-inverse); }

  /* Panel: prize image over the form card. `:global` because the class is passed
     INTO PhotoSlot, so the element carries PhotoSlot's scope id, not this page's
     (Astro scoping gotcha). The class is unique to this page, so :global is safe. */
  .hero__panel :global(.hero__prize) { margin-bottom: 16px; }
  .hero__form {
    background: var(--white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xl);
    padding: 24px 22px;
  }
  .hero__formhead { font-size: 17px; font-weight: var(--weight-bold); color: var(--text-heading); }
  /* Single column at every breakpoint: single-column forms carry less friction
     and convert better (change ticket 3.3). */
  .hero__fields { display: grid; grid-template-columns: 1fr; gap: 14px; margin-top: 16px; }
  .hf { display: flex; flex-direction: column; gap: 5px; margin: 0; }
  .hf label { font-size: var(--text-sm); font-weight: var(--weight-semibold); color: var(--text-heading); }
  .hf__req { color: var(--cta); }
  .hf input {
    font-family: var(--font-body); font-size: var(--text-base); color: var(--text-heading);
    background: var(--white); border: 1px solid var(--border-default);
    border-radius: var(--radius-md); padding: 11px 14px; width: 100%;
    transition: border-color var(--duration-fast) var(--ease-standard), box-shadow var(--duration-fast) var(--ease-standard);
  }
  .hf input:focus {
    outline: none; border-color: var(--focus-ring);
    box-shadow: 0 0 0 3px color-mix(in oklch, var(--focus-ring) 22%, transparent);
  }
  .hero__form :global(.btn) { margin-top: 18px; }
  .hero__formmicro { margin-top: 12px; font-size: var(--text-xs); line-height: var(--leading-relaxed); color: var(--text-muted); }
  .hero__formmicro a { color: var(--text-link); font-weight: var(--weight-semibold); text-decoration: underline; white-space: nowrap; }

  .hero__foot { display: flex; flex-direction: column; gap: 12px; margin-top: 8px; }
  .hero__trust { display: flex; align-items: center; gap: 8px; font-size: 13.5px; font-weight: var(--weight-semibold); color: var(--text-body-inverse); }
  .hero__trust :global(svg) { color: var(--page-accent-on-dark, var(--orange-400)); flex: none; }
  .hero__cond { font-size: 12px; line-height: var(--leading-relaxed); color: var(--text-muted-inverse); max-width: 62ch; }
  .hero__cond a { color: var(--page-accent-on-dark, var(--orange-400)); font-weight: var(--weight-semibold); text-decoration: underline; }

  /* ---- shared: CTA row ------------------------------------------------- */
  .ctarow { display: flex; flex-wrap: wrap; align-items: center; gap: 16px 20px; margin-top: 40px; }
  .ctarow__note { font-size: 13.5px; font-weight: var(--weight-bold); letter-spacing: var(--tracking-wide); text-transform: uppercase; color: var(--page-accent-ink, var(--orange-800)); }
  .ctarow--dark { margin-top: 36px; position: relative; }
  .ctarow--dark .ctarow__note { color: var(--page-accent-on-dark, var(--orange-400)); }

  /* ---- 2. HOW IT WORKS ------------------------------------------------- */
  .how__head { max-width: 640px; margin-bottom: 36px; }
  .how__steps { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
  .how__step {
    background: var(--surface-sunken); border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg); padding: 26px 24px 24px;
  }
  .how__num {
    display: inline-flex; align-items: center; justify-content: center;
    width: 40px; height: 40px; border-radius: var(--radius-signature);
    background: var(--page-accent, var(--orange-500)); color: var(--ink-500);
    font-size: 19px; font-weight: var(--weight-bold);
  }
  .how__t { margin-top: 16px; font-size: 17.5px; font-weight: var(--weight-semibold); line-height: var(--leading-snug); color: var(--text-heading); }
  .how__d { margin-top: 8px; font-size: 14.5px; line-height: var(--leading-relaxed); color: var(--text-body); }

  .postal {
    display: grid; grid-template-columns: 1.4fr 0.6fr; gap: 32px; align-items: center;
    margin-top: 24px; padding: 22px 24px;
    background: var(--white); border: 1px dashed var(--border-default); border-radius: var(--radius-lg);
  }
  .postal__t { font-size: 16px; font-weight: var(--weight-semibold); color: var(--text-heading); }
  .postal__d { margin-top: 8px; font-size: 14px; line-height: var(--leading-relaxed); color: var(--text-body); }
  .postal__d strong { color: var(--text-heading); }
  .postal__addr {
    font-style: normal; font-size: 14px; line-height: var(--leading-relaxed);
    color: var(--text-heading); font-weight: var(--weight-medium);
    padding-left: 22px; border-left: 3px solid var(--page-accent, var(--orange-500));
  }

  /* ---- 3. WHY ABBEY ---------------------------------------------------- */
  .why { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: start; }
  .why__lead { margin-top: 16px; font-size: var(--text-base); line-height: var(--leading-relaxed); color: var(--text-body); }
  .why__facts { display: flex; flex-direction: column; gap: 12px; margin-top: 22px; }
  .why__facts li { display: flex; gap: 12px; align-items: flex-start; font-size: 14.5px; color: var(--text-body); }
  .why__facts :global(svg) { color: var(--page-accent-ink, var(--orange-800)); flex: none; margin-top: 2px; }
  .why__reviews { display: flex; flex-direction: column; gap: 16px; }
  .why__review { background: var(--white); border: 1px solid var(--border-subtle); border-radius: var(--radius-lg); padding: 22px 24px; box-shadow: var(--shadow-sm); }
  .why__quote { font-size: 15px; line-height: var(--leading-relaxed); color: var(--text-heading); }
  .why__by { margin-top: 12px; font-size: var(--text-sm); color: var(--text-muted); }
  .why__by span { font-weight: var(--weight-bold); color: var(--text-heading); }

  /* ---- 4. OFFER (dark band) -------------------------------------------- */
  .offer { position: relative; overflow: hidden; padding-block: 80px; background: radial-gradient(900px 560px at 82% 25%, color-mix(in oklch, var(--page-accent-glow, var(--orange-900)) 55%, transparent) 0%, transparent 65%); }
  .offer__mark { position: absolute; left: -100px; top: 42%; transform: translateY(-50%); width: 360px; opacity: 0.1; pointer-events: none; }
  .offer__inner { position: relative; display: grid; grid-template-columns: 1.05fr 0.95fr; gap: 56px; align-items: center; }
  .offer__copy { display: flex; flex-direction: column; gap: 18px; align-items: flex-start; }
  .offer__h2 { font-family: var(--font-display); font-size: clamp(28px, 3vw, 42px); color: var(--white); text-wrap: balance; }
  .offer__lead { font-size: var(--text-md); line-height: var(--leading-relaxed); color: var(--text-body-inverse); max-width: 540px; }
  .offer__guar {
    font-size: var(--text-base); line-height: var(--leading-relaxed); color: var(--white); max-width: 540px;
    padding-left: 16px; border-left: 2px solid var(--orange-400);
  }
  .offer__pricing { font-size: var(--text-sm); color: var(--text-muted-inverse); }
  .offer__card {
    background: color-mix(in oklch, var(--white) 7%, transparent);
    border: 1px solid color-mix(in oklch, var(--orange-400) 45%, transparent);
    border-radius: var(--radius-xl); box-shadow: var(--shadow-xl); padding: 28px; backdrop-filter: blur(6px);
  }
  .offer__row { display: flex; flex-direction: column; gap: 2px; }
  .offer__row--now, .offer__row--plan { padding-top: 18px; margin-top: 16px; border-top: 1px solid var(--border-inverse); }
  .offer__label { font-size: 12.5px; font-weight: var(--weight-semibold); letter-spacing: var(--tracking-wide); text-transform: uppercase; color: var(--text-muted-inverse); }
  .offer__num { font-size: 40px; font-weight: var(--weight-bold); line-height: 1.05; color: var(--white); }
  .offer__row--was .offer__num { font-size: 28px; color: var(--text-muted-inverse); text-decoration: line-through; text-decoration-color: color-mix(in oklch, var(--white) 45%, transparent); }
  .offer__row--now .offer__num { color: var(--orange-300); font-size: 48px; }
  .offer__mo { font-size: 20px; font-weight: var(--weight-semibold); color: var(--text-muted-inverse); }
  .offer__then { font-size: var(--text-sm); color: var(--text-body-inverse); margin-top: 2px; }

  /* ---- FORM SLOT (final EnquiryForm) ---------------------------------- */
  .ef__risk { display: flex; flex-direction: column; gap: 9px; margin-top: 4px; }
  .ef__risk li { display: flex; gap: 10px; align-items: center; font-size: 14.5px; font-weight: var(--weight-medium); color: var(--text-heading); }
  .ef__risk :global(svg) { color: var(--page-accent-ink, var(--orange-800)); flex: none; }
  .ef__talk { font-size: var(--text-sm); line-height: var(--leading-relaxed); color: var(--text-muted); }
  .ef__talk a { color: var(--text-link); font-weight: var(--weight-bold); white-space: nowrap; }

  /* ---- DRAW TERMS ------------------------------------------------------ */
  .drawterms { max-width: 820px; }
  .drawterms__h2 { font-size: clamp(24px, 2.2vw, 30px); color: var(--text-heading); }
  .drawterms__line { margin-top: 14px; font-size: var(--text-base); font-weight: var(--weight-medium); color: var(--text-heading); }
  .drawterms__list { margin-top: 16px; display: flex; flex-direction: column; gap: 10px; }
  .drawterms__list li { padding-left: 18px; position: relative; font-size: 14px; line-height: var(--leading-relaxed); color: var(--text-body); }
  .drawterms__list li::before { content: ''; position: absolute; left: 0; top: 9px; width: 6px; height: 6px; border-radius: 50%; background: var(--page-accent, var(--orange-500)); }

  /* ---- RESPONSIVE ------------------------------------------------------ */
  @media (max-width: 1024px) {
    .hero__grid { grid-template-columns: 1fr; grid-template-areas: "lead" "panel" "foot"; row-gap: 24px; }
    .hero__sub { max-width: none; }
    /* Hero stacked: form first, prize image below it, so the form sits right
       under the deadline and is reachable without scrolling (ticket 1.4). */
    .hero__form { order: 1; }
    .hero__panel :global(.hero__prize) { order: 2; margin-bottom: 0; margin-top: 16px; }
    .offer__inner { grid-template-columns: 1fr; gap: 32px; }
    .why { grid-template-columns: 1fr; gap: 32px; }
  }
  @media (max-width: 860px) {
    .how__steps { grid-template-columns: 1fr; }
    .postal { grid-template-columns: 1fr; gap: 16px; }
    .postal__addr { padding-left: 0; padding-top: 14px; border-left: 0; border-top: 1px solid var(--border-subtle); }
  }
  @media (max-width: 700px) {
    .hero { padding-top: 88px; }
    /* Compact prize image so the form and deadline stay above the fold.
       :global for the same reason as above (class passed into PhotoSlot). */
    .hero__panel :global(.hero__prize) { aspect-ratio: auto !important; height: 116px; }
  }
</style>

<script>
  /* Countdown to the draw close. The `.cd__label` text is the no-JS fallback;
     this fills the day/hour/minute boxes and reveals them, and switches the
     label to a closed state once the deadline passes. Date.now() is fine in a
     browser page script. */
  (function () {
    const el = document.querySelector<HTMLElement>('.cd');
    if (!el) return;
    const iso = el.getAttribute('data-deadline');
    if (!iso) return;
    const target = Date.parse(iso);
    if (Number.isNaN(target)) return;

    const timer = el.querySelector<HTMLElement>('.cd__timer');
    const label = el.querySelector<HTMLElement>('.cd__label');
    const dEl = el.querySelector<HTMLElement>('.cd__d');
    const hEl = el.querySelector<HTMLElement>('.cd__h');
    const mEl = el.querySelector<HTMLElement>('.cd__m');
    if (!timer || !label || !dEl || !hEl || !mEl) return;

    function tick() {
      let secs = Math.floor((target - Date.now()) / 1000);
      if (secs <= 0) {
        label.textContent = 'Entries have closed';
        timer.hidden = true;
        return false;
      }
      const days = Math.floor(secs / 86400); secs -= days * 86400;
      const hrs = Math.floor(secs / 3600); secs -= hrs * 3600;
      const mins = Math.floor(secs / 60);
      dEl.textContent = String(days);
      hEl.textContent = String(hrs);
      mEl.textContent = String(mins);
      timer.hidden = false;
      return true;
    }

    if (tick()) {
      const id = setInterval(() => { if (!tick()) clearInterval(id); }, 30000);
    }
  })();
</script>
